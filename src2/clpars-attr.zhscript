加载lib/clpars4。

定义显属性以下代码
	定义测、号、有名以下代码
		赋予名以属性‘号’。
		显示“{‘名’ : "‘‘名’’"}”先如果‘有名’那么“ /*‘属性名‘号’’*/”了换行。
	上代码。
	循环【次】先
		如果不存在属性‘次’那么跳出。
		测‘次’、1。
	了。
	测flag。
	测tag。
	测rem。
	测type。
	测argc。
	测from。
	测code。
	测no。
	测i。
上代码。
赋予解以调用‘命令行加回调’、、
	-h | --help、帮助、0、下代码
		（显属性。）		
		显示先调用‘命令行帮助’、‘解’了换行。
	上代码、
	-、、ba、下代码
		如果‘属性no’大于1那么返回。
		显示‘参数0’--‘参数’换行。
	上代码、
	、、1、下代码
		显示‘属性i’‘参数0’换行。
	上代码、
	(甲)()(乙)、正则表达、r2、下代码
		显示‘参数’换行。
		显属性。
	上代码。
调用‘命令行解析’、‘解’、-h、甲、乙、丙、丁、甲乙、丙、丁、-甲、乙、-丙、--丁。
