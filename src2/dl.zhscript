解释下代码
	模块“标准库”。

	函数集
	“libstdc++.so.6.0.13”、
	“libstdc++.so.6”、
	“libstdc++-libc6.1-1.so.2”、
	“libstdc++.dylib”。

	函数“得当前目录”以“getcwd”、“-S”。
	函数“格输出”以“printf”、“i-c*”。
上代码。

显示当前目录先调用‘得当前目录’了换行。

调用‘格输出’-c-i-l-lu-c*-f、“%c %d %ld %lu %s %f”换行、c、21、-2100000000、4200000000、string、3.567。
